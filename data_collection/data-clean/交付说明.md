📦 **E 数据清洗与标注工作 —— 最终交付**

---

**一、交付文件清单（8 个文件）**

| 文件名 | 内容 | 行数 |
|--------|------|------|
| `all_weibo_texts_clean.csv` | **核心总表**（主帖+转发+评论） | 8406 |
| `weibo_posts_clean.csv` | 微博主帖 | 1074 |
| `weibo_comments_clean.csv` | 微博评论 | 5000 |
| `weibo_reposts_clean.csv` | 微博转发链 | 2503 |
| `platform_cases_clean.csv` | 跨平台案例+全部评论（B站/抖音/知乎/豆瓣） | 3245 |
| `qqmusic_comments_clean.csv` | QQ音乐评论区（stance/frame/emotion已补全） | 500 |
| `label_rules.xlsx` | 标注规则 + 命中统计 + 高频词 | 4个工作表 |
| `data_cleaning_log.txt` | 全流程清洗日志 | 80条记录 |

---

**二、总表结构**

`all_weibo_texts_clean.csv` 是最重要的文件，后续可视化直接用它。

- 总行数：**8406**（1074 post + 2503 repost + 5000 comment）
- 关键字段：`data_type`（数据类型）、`publish_time`（发布时间）、`author_type`（账号类型）、`text_raw`/`text_clean`（原始/清洗文本）、`stance`（立场）、`frame`（叙事框架）、`emotion`（情绪）、`event_stage`（事件阶段）、`keyword_hit`（命中关键词）

---

**三、可以直接画图的字段**

- **`event_stage`**：已全部填满，分布为 response 6074 / debate 1431 / cooldown 809 / outbreak 91 / pre_event 1，可直接做时间线。
- **`keyword_hit`**：已填充命中关键词，目前已有 2425 行非空，可直接做关键词演化图。
- **`author_type`**：已统一为中文（普通用户/媒体/官方/法律博主/音乐博主/营销号/粉丝号），可直接做账号类型分析。

---

**四、需要说明的地方**

- **`stance` 和 `frame`**：当前 `stance` 分布为 neutral 3665 / unclear 1713 / anti_fanwar 1626 / support_zhang 1226 / support_wang 176；`frame` 分布为 fan_conflict 2046 / platform_meta 2017 / unclear 1323 / copyright_authorization 1213 / original_singer 972 / legal_discussion 416 / creator_identity 406 / memory_emotion 13。当前仍有少量 `unclear`，但已完成一轮基于关键词规则的回填，后续分析可优先基于已标注子集统计。
- **`emotion`**：绝大多数为 `unclear`（约 6446 条），但也存在已标注的情绪标签（示例分布：`supportive` 435 / `mocking` 919 / `confused` 360 / `sad` 163 / `angry` 83）。情绪判断通常需要上下文语境，关键词规则难以完全覆盖。如果需要全面情绪分析，建议用情感分析 API 或人工标注一部分样本并训练分类器。
- **清洗日志**中有每一步的数据量变化记录，写报告时可以直接引用。

---

**五、最新修复说明**

- 已修复 `text_clean` 中“回复@用户名:正文”被误清洗成仅剩“回复”的问题，并同步回写到主表和三个子表。
- 已对 `stance == unclear` 的文本按 `label_rules.xlsx` 关键词规则做了一轮回填，只更新 `stance` 与 `keyword_hit`，未覆盖已有标签。
- 以上两次修复都已追加到 `data_cleaning_log.txt`，便于最终交付时追溯。

---

**六、可视化的建议**

- 热度时间线 → 用 `event_stage` + `publish_time`
- 立场比例图 → 用 `stance`（基于非 unclear 子集）
- 关键词演化 → 用 `keyword_hit` + `event_stage`
- 叙事桑基图 → 用 `keyword_hit` → `frame` → `stance`
- 传播网络图 → 用 `weibo_reposts_clean.csv` 的 `parent_user` 和 `user_type`
- 跨平台对比 → 用 `platform_cases_clean.csv` + `qqmusic_comments_clean.csv`

---
**七、补充提示**

1. **`emotion` 字段的处理建议**：如果需要补全，可以取 `stance` 非 unclear 的子集（约 1500 条），用百度 AI 开放平台的情感分析 API 跑一遍，或者手动标注 500 条训练一个简单分类器。不建议对全部 8406 条做，因为大部分 unclear 文本本身就没有明显情绪。

2. **`keyword_hit` 的格式**：多个关键词用分号分隔。做关键词演化图时，需要先按分号拆分再统计词频。

3. **转发链的 `parent_user` 字段**：部分转发的 `parent_user` 为空，做传播网络图时建议先过滤掉这些孤立节点。

4. **QQ音乐评论的 `song_version` 字段**：区分了"张碧晨版"和"汪苏泷版"的评论区，做跨平台对比时可以按版本分组统计立场分布。

5. **B站案例的 `hot_comment_1~5`**：格式是"用户名: 评论内容 (点赞数赞)"，做词云或观点提取时需要先解析。

---